---
- name: Install mise-managed tools
  ansible.builtin.command: "{{ mac_dev_playbook_brew_prefix }}/bin/mise install"
  register: mac_dev_playbook_mise_install
  changed_when: "'installed' in mac_dev_playbook_mise_install.stdout"
